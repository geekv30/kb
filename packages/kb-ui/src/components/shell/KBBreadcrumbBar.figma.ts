export const figmaNode = {
  fileKey: '9aGp5t9fH1d0PXi4LMhOdb',
  // Canonical 1280×54 editor breadcrumb bar with Save-as-draft / Publish /
  // Close action trio + home (sidebar-collapsed) glyph. Lives inside the
  // `editor-content-area` page (50:7076) → `article-publish-01` (50:8223)
  // → `option 10` (50:8224) → `Container` (50:8395).
  nodeId: '50:8395',
  screenshotName: 'kb-breadcrumb-bar',
} as const;
