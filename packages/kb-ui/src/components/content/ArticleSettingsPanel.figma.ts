export const figmaNode = {
  fileKey: '9aGp5t9fH1d0PXi4LMhOdb',
  // Canonical 452×356 expanded settings panel (`settings.config - expanded`
  // 53:8384) inside the `editor-config` page (53:2458). The Figma frame
  // shows only 3 dropdowns (Author, Category, Article Slug) as a snippet;
  // the full production panel renders 8 fields per PRD §13. Side-by-side
  // height drift is expected and called out in the review report.
  nodeId: '53:8384',
  screenshotName: 'article-settings-panel',
} as const;
